export interface ICategoryRequest {
    name: string;
    
}

export interface ICategory {
    id: number;
    name: string;
    totalClients: number;
}

export interface ICategoryDropdown {
    id: number;
    name: string;
}